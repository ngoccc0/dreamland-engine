import type { TranslationKey } from "@/lib/i18n";
import type { ItemDefinition } from "../types";

export const itemDefinitions: Record<TranslationKey, ItemDefinition> = {
    // This is a placeholder. The actual item definitions are now in a separate, dedicated file
    // to keep this structure clean. See `src/lib/game/items.ts`.
};
