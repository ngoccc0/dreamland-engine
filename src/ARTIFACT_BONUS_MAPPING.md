/**
 * ARTIFACT BONUS FIELD MAPPING REFERENCE
 *
 * This file documents how artifact set bonuses map to CharacterStats.
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * FIELD MAPPING
 * ═══════════════════════════════════════════════════════════════════════════\n *\n * Artifact Bonus Fields → CharacterStats Fields\n *\n * ┌─────────────────────────────────────────────────────────────────────────┐\n * │ ARTIFACT FIELD │ DESCRIPTION            │ MAPS TO      │ FORMULA       │\n * ├─────────────────────────────────────────────────────────────────────────┤\n * │ str            │ Strength bonus         │ strength     │ Direct copy   │\n * │ dex            │ Dexterity bonus        │ dexterity    │ Direct copy   │\n * │ con            │ Constitution bonus     │ vitality     │ Renamed       │\n * │ int            │ Intelligence bonus     │ intelligence │ Direct copy   │\n * │ wis            │ Wisdom bonus           │ luck         │ Indirect      │\n * │ cha            │ Charisma bonus         │ luck         │ Stacks with   │\n * └─────────────────────────────────────────────────────────────────────────┘\n *\n * ═══════════════════════════════════════════════════════════════════════════\n * DETAILED MAPPINGS\n * ═══════════════════════════════════════════════════════════════════════════\n *\n * **str → strength**\n *   Direct one-to-one mapping\n *   No conversion needed\n *   Example: artifact.bonuses.str = 5 → character stats +5 STR\n *\n * **dex → dexterity**\n *   Direct one-to-one mapping\n *   No conversion needed\n *   Example: artifact.bonuses.dex = 3 → character stats +3 DEX\n *\n * **con → vitality**\n *   \"Constitution\" field from artifact maps to \"vitality\" stat\n *   This is because CharacterStats uses 5 stats (not 6)\n *   Example: artifact.bonuses.con = 8 → character stats +8 VIT\n *\n * **int → intelligence**\n *   Direct one-to-one mapping\n *   No conversion needed\n *   Example: artifact.bonuses.int = 7 → character stats +7 INT\n *\n * **wis → luck** (Indirect)\n *   Wisdom bonus maps to luck (no dedicated wisdom stat)\n *   This is because CharacterStats has no \"wisdom\" field\n *   Use case: Wisdom items increase \"detection\" via luck\n *   Example: artifact.bonuses.wis = 4 → character stats +4 LCK\n *   Formula: luck += wis (additive)\n *\n * **cha → luck** (Stacking)\n *   Charisma bonus also maps to luck\n *   Stacks with any wisdom bonus from same set\n *   Use case: Charisma items increase \"social fortune\" via luck\n *   Example: artifact.bonuses.cha = 2 → character stats +2 LCK\n *   Formula: luck += cha (additive to previous luck)\n *\n * ═══════════════════════════════════════════════════════════════════════════\n * IMPLEMENTATION IN usePlayerStats\n * ═══════════════════════════════════════════════════════════════════════════\n *\n * ```typescript\n * // In usePlayerStats function:\n *\n * if (unlockedSets && unlockedSets.length > 0) {\n *   for (const set of unlockedSets) {\n *     if (set.bonuses) {\n *       const artifactBonus: Partial<CharacterStats> = {};\n *\n *       // Direct mappings\n *       if (set.bonuses.str !== undefined) artifactBonus.strength = set.bonuses.str;\n *       if (set.bonuses.dex !== undefined) artifactBonus.dexterity = set.bonuses.dex;\n *       if (set.bonuses.con !== undefined) artifactBonus.vitality = set.bonuses.con;\n *       if (set.bonuses.int !== undefined) artifactBonus.intelligence = set.bonuses.int;\n *\n *       // Indirect mappings (stack onto luck)\n *       if (set.bonuses.wis !== undefined) {\n *         artifactBonus.luck = (artifactBonus.luck ?? 0) + set.bonuses.wis;\n *       }\n *       if (set.bonuses.cha !== undefined) {\n *         artifactBonus.luck = (artifactBonus.luck ?? 0) + set.bonuses.cha;\n *       }\n *\n *       // Apply bonuses\n *       applyBonusToStats(stats, artifactBonus, breakdown.artifacts);\n *     }\n *   }\n * }\n * ```\n *\n * ═══════════════════════════════════════════════════════════════════════════\n * EXAMPLES OF ARTIFACT SETS\n * ═══════════════════════════════════════════════════════════════════════════\n *\n * **\"Elemental Mastery\" (3 artifacts)**\n * ```typescript\n * {\n *   setName: 'Elemental Mastery',\n *   bonuses: {\n *     str: 5,\n *     int: 7,\n *     dex: 3\n *   },\n *   artifactCount: 3\n * }\n *\n * Applies to player stats:\n *   - strength: +5\n *   - intelligence: +7\n *   - dexterity: +3\n * ```\n *\n * **\"Ancient Guardian\" (4 artifacts)**\n * ```typescript\n * {\n *   setName: 'Ancient Guardian',\n *   bonuses: {\n *     con: 10,    // Maps to vitality\n *     wis: 5,     // Maps to luck\n *     cha: 3      // Also maps to luck\n *   },\n *   artifactCount: 4\n * }\n *\n * Applies to player stats:\n *   - vitality: +10\n *   - luck: +5 (from wis) + 3 (from cha) = +8\n * ```\n *\n * **\"Shadow Dancer\" (2 artifacts)**\n * ```typescript\n * {\n *   setName: 'Shadow Dancer',\n *   bonuses: {\n *     dex: 12,\n *     str: 4,\n *     wis: 2\n *   },\n *   artifactCount: 2\n * }\n *\n * Applies to player stats:\n *   - dexterity: +12\n *   - strength: +4\n *   - luck: +2\n * ```\n *\n * ═══════════════════════════════════════════════════════════════════════════\n * DESIGN RATIONALE\n * ═══════════════════════════════════════════════════════════════════════════\n *\n * **Why only 5 CharacterStats?**\n *   - Simpler combat calculations\n *   - Easier balance and debugging\n *   - Covers all game mechanics:\n *     * strength: Physical damage, carrying, weapon effectiveness\n *     * dexterity: Accuracy, evasion, critical chance\n *     * intelligence: Magical power, mana, spell slots\n *     * vitality: Health, defense, status resistance\n *     * luck: Critical multiplier, rare drops, escape chance\n *\n * **Why map con→vitality?**\n *   - \"Constitution\" is the classic D&D term for \"health stat\"\n *   - \"Vitality\" is our game's equivalent\n *   - Keeps data at source layer (artifact) simple\n *   - Translation layer happens at hooks\n *\n * **Why map wis/cha→luck?**\n *   - No dedicated Wisdom or Charisma stat in CharacterStats\n *   - Both relate to \"fortune\" and \"social advantage\"\n *   - Luck is the closest conceptual match\n *   - Alternative design could use attributes map, but simpler this way\n *\n * **Could we add mor stats?**\n *   - Would require CharacterStats interface change (breaking)\n *   - Would complicate balance (6+ stats = exponential complexity)\n *   - Current 5 stats cover all RPG mechanics\n *   - Future: Could add \"focused builds\" via enchantment system\n *\n * ═══════════════════════════════════════════════════════════════════════════\n * FUTURE EXTENSIBILITY\n * ═══════════════════════════════════════════════════════════════════════════\n *\n * If CharacterStats is extended in future:\n *\n * **Option A: Add wisdom stat**\n *   ```typescript\n *   interface CharacterStats {\n *     strength: number;\n *     dexterity: number;\n *     intelligence: number;\n *     vitality: number;\n *     wisdom: number;        // NEW\n *     luck: number;\n *   }\n *\n *   // Then mapping becomes:\n *   if (set.bonuses.wis !== undefined) artifactBonus.wisdom = set.bonuses.wis;\n *   ```\n *\n * **Option B: Add charisma stat**\n *   ```typescript\n *   interface CharacterStats {\n *     strength: number;\n *     dexterity: number;\n *     intelligence: number;\n *     vitality: number;\n *     luck: number;\n *     charisma: number;      // NEW\n *   }\n *\n *   // Then mapping becomes:\n *   if (set.bonuses.cha !== undefined) artifactBonus.charisma = set.bonuses.cha;\n *   ```\n *\n * **Option C: Add both**\n *   - Would require new interface version\n *   - Update usePlayerStats to handle both old and new artifacts\n *   - Maintain backward compatibility\n *\n * ═══════════════════════════════════════════════════════════════════════════\n * TESTING ARTIFACT BONUSES\n * ═══════════════════════════════════════════════════════════════════════════\n *\n * ```typescript\n * test('artifact set bonuses apply correctly', () => {\n *   const character = new Character('test', 'Hero', {\n *     strength: 10, dexterity: 10, intelligence: 10,\n *     vitality: 10, luck: 5\n *   });\n *\n *   const elementalSet: UnlockedArtifactSet = {\n *     setName: 'Elemental',\n *     bonuses: { str: 5, int: 7 },\n *     artifactCount: 3\n *   };\n *\n *   const stats = usePlayerStats(character, [elementalSet]);\n *\n *   expect(stats.strength).toBe(15);      // 10 + 5\n *   expect(stats.intelligence).toBe(17);  // 10 + 7\n *   expect(stats.breakdown.artifacts.strength).toBe(5);\n * });\n *\n * test('wis and cha bonus stack on luck', () => {\n *   const guardianSet: UnlockedArtifactSet = {\n *     setName: 'Guardian',\n *     bonuses: { con: 10, wis: 5, cha: 3 },\n *     artifactCount: 4\n *   };\n *\n *   const stats = usePlayerStats(character, [guardianSet]);\n *\n *   expect(stats.vitality).toBe(20);  // 10 + 10\n *   expect(stats.luck).toBe(13);      // 5 + 5 (wis) + 3 (cha)\n * });\n * ```\n *\n */\n\n// This file is for documentation only.\n// The mapping is implemented in src/hooks/usePlayerStats.ts\n