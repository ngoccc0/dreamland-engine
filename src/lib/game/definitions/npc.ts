import type { TranslationKey } from "@/lib/i18n";
import type { Npc } from "../types";

export const npcs: Record<TranslationKey, Npc> = {
  // This is a placeholder. NPC definitions will be loaded from biome templates.
};
