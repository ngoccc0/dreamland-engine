# This is an example file to show which environment variables are needed.
# DO NOT COMMIT YOUR ACTUAL KEYS TO GIT.
# Create a .env file in the project root and add your keys there for local development.
# When deploying, add these variables to your hosting provider's environment settings.

# =================================================
# Firebase - Client-side (Publicly accessible)
# These are prefixed with NEXT_PUBLIC_ and are safe to expose in the browser.
# =================================================
NEXT_PUBLIC_FIREBASE_API_KEY="YOUR_FIREBASE_API_KEY"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="YOUR_PROJECT_ID.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="YOUR_PROJECT_ID"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="YOUR_PROJECT_ID.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="YOUR_SENDER_ID"
NEXT_PUBLIC_FIREBASE_APP_ID="YOUR_APP_ID"

# =================================================
# Genkit AI Models - Server-side (SECRET)
# These keys are ONLY used on the server and are NOT exposed to the browser.
# You MUST add these to your hosting provider's environment variables.
# =================================================
GEMINI_API_KEY_PRIMARY="YOUR_GEMINI_API_KEY_HERE"
GEMINI_API_KEY_SECONDARY="" # Optional: for fallback or increased rate limits
OPENAI_API_KEY="" # Optional: if you want to use GPT models
DEEPSEEK_API_KEY="" # Optional: if you want to use Deepseek models
