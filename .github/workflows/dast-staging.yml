name: DAST Staging
on: workflow_dispatch

jobs:
  placeholder:
    name: Placeholder job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder step
        run: echo "DAST staging placeholder workflow — no-op"
